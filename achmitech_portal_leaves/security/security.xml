<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Portal clients can read only leaves where they are the client_partner_id -->
    <record id="hr_leave_rule_portal_client" model="ir.rule">
        <field name="name">HR Leave: portal client — own interims only</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">[('client_partner_id', '=', user.partner_id.id)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal interims can read only their own leave requests -->
    <record id="hr_leave_rule_portal_interim" model="ir.rule">
        <field name="name">HR Leave: portal interim — own leaves only</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>
</odoo>
